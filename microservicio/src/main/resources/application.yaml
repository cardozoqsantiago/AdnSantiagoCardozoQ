jasypt:
   encryptor:
      password: ${jasyptpwd:secretkey}
      algorithm: PBEWithMD5AndDES

server:
   port: 8080
   servlet:
      context-path: /AdnSantiagoCardozoQ
      session:
         timeout: 21600s

spring:
   datasource:
      type: com.zaxxer.hikari.HikariDataSource
      jdbcUrl: jdbc:mysql://servermysqlcqs.mysql.database.azure.com:3306/adn?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
      username: adnRoot@servermysqlcqs
      password: 964Mwr964
      hikari:
         poolName: pool
